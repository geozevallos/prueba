Esto es un código en línea: `Array.prototype.map()`
 
 
 Se supone que esto es código:

```
var add2 = function(number) {
  return number + 2;
}
```

```js
var add2 = function(number) {
  return number + 2;
}
```

